import CategoryManager from "@/components/admin/category-manager";import prisma from "@/lib/db";export const dynamic="force-dynamic";export default async function CategoriesPage(){const categories=await prisma.category.findMany({orderBy:{name:"asc"},include:{_count:{select:{posts:true}}}});return <section className="space-y-7"><div><p className="text-sm font-medium text-violet-500">Organización</p><h1 className="mt-1 text-3xl font-bold">Categorías</h1><p className="mt-2 text-sm text-muted-foreground">Crea áreas temáticas y rutas de aprendizaje.</p></div><CategoryManager categories={categories}/></section>}
