import SeriesManager from "@/components/admin/series-manager";import prisma from "@/lib/db";export const dynamic="force-dynamic";export default async function SeriesPage(){const series=await prisma.series.findMany({orderBy:{updatedAt:"desc"},include:{_count:{select:{posts:true}}}});return <section className="space-y-7"><div><p className="text-sm font-medium text-violet-500">Aprendizaje estructurado</p><h1 className="mt-1 text-3xl font-bold">Series y cursos</h1><p className="mt-2 text-sm text-muted-foreground">Divide temas amplios en partes que el estudiante pueda recorrer paso a paso.</p></div><SeriesManager series={series}/></section>}
