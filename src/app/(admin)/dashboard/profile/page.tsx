import ProfileForm from "@/components/admin/profile-form";import prisma from "@/lib/db";import {authOptions} from "@/lib/options";import {getServerSession} from "next-auth";import {redirect} from "next/navigation";export const dynamic="force-dynamic";export default async function ProfilePage(){const session=await getServerSession(authOptions);if(!session?.user?.id)redirect("/login");const user=await prisma.user.findUnique({where:{id:session.user.id},select:{name:true,email:true,role:true,bio:true,avatarUrl:true,jobTitle:true,website:true,githubUrl:true,linkedinUrl:true}});if(!user)redirect("/login");return <section className="space-y-7"><div><p className="text-sm font-medium text-violet-500">Identidad del instructor</p><h1 className="mt-1 text-3xl font-bold">Mi perfil</h1><p className="mt-2 text-sm text-muted-foreground">Esta información acompaña tus artículos, clases y cursos.</p></div><ProfileForm initial={user}/></section>}
