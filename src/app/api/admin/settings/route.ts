import { authOptions } from "@/lib/options";import prisma from "@/lib/db";import {getServerSession} from "next-auth";import {NextResponse} from "next/server";
export async function PUT(req:Request){const session=await getServerSession(authOptions);if(!session?.user?.id||session.user.role!=="ADMIN")return NextResponse.json({message:"No autorizado"},{status:403});const data=await req.json();const settings=await prisma.platformSettings.upsert({where:{id:"main"},create:{id:"main",academyName:data.academyName,tagline:data.tagline,description:data.description||null,baseUrl:data.baseUrl,senderName:data.senderName,senderEmail:data.senderEmail,replyToEmail:data.replyToEmail||null,notifyOnPublish:!!data.notifyOnPublish},update:{academyName:data.academyName,tagline:data.tagline,description:data.description||null,baseUrl:data.baseUrl,senderName:data.senderName,senderEmail:data.senderEmail,replyToEmail:data.replyToEmail||null,notifyOnPublish:!!data.notifyOnPublish}});return NextResponse.json(settings)}
