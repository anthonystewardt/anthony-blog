"use client";
import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { FolderTree,Plus } from "lucide-react";import { useRouter } from "next/navigation";import { useState } from "react";import { toast } from "sonner";
export default function CategoryManager({categories}:{categories:Array<{id:string;name:string;description:string|null;color:string;_count:{posts:number}}>}){const router=useRouter();const[name,setName]=useState("");const[description,setDescription]=useState("");const[saving,setSaving]=useState(false);async function create(){setSaving(true);const res=await fetch("/api/categories",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,description})});if(res.ok){toast.success("Categoría creada");setName("");setDescription("");router.refresh()}else toast.error("No se pudo crear");setSaving(false)}return <div className="grid gap-6 xl:grid-cols-[1fr_360px]"><div className="overflow-hidden rounded-2xl border bg-card">{categories.length?categories.map(c=><div key={c.id} className="flex items-center gap-4 border-b p-5 last:border-0"><div className="grid size-11 place-items-center rounded-xl bg-violet-500/10 text-violet-500"><FolderTree className="size-5"/></div><div className="flex-1"><p className="font-semibold">{c.name}</p><p className="text-sm text-muted-foreground">{c.description||"Sin descripción"}</p></div><span className="rounded-full bg-muted px-3 py-1 text-xs">{c._count.posts} contenidos</span></div>):<p className="p-12 text-center text-sm text-muted-foreground">Crea la primera categoría de tu academia.</p>}</div><aside className="h-fit rounded-2xl border bg-card p-5"><h2 className="font-bold">Nueva categoría</h2><p className="mt-1 text-sm text-muted-foreground">Organiza contenidos por temas o rutas.</p><div className="mt-5 space-y-4"><Input value={name} onChange={e=>setName(e.target.value)} placeholder="Ej. Desarrollo web"/><Input value={description} onChange={e=>setDescription(e.target.value)} placeholder="Descripción breve"/><Button onClick={create} disabled={saving||!name.trim()} className="w-full bg-violet-600 text-white"><Plus className="mr-2 size-4"/> Crear categoría</Button></div></aside></div>}
