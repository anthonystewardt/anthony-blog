"use client";import {Button} from "@/components/ui/button";import {Input} from "@/components/ui/input";import {Textarea} from "@/components/ui/textarea";import {BellRing,Globe2,Mail,Save,ShieldCheck} from "lucide-react";import {useState} from "react";import {toast} from "sonner";
type Settings={academyName:string;tagline:string;description:string|null;baseUrl:string;senderName:string;senderEmail:string;replyToEmail:string|null;notifyOnPublish:boolean};
export default function PlatformSettingsForm({initial}:{initial:Settings}){const[form,setForm]=useState(initial);const[saving,setSaving]=useState(false);const set=(key:keyof Settings,value:string|boolean)=>setForm(f=>({...f,[key]:value}));async function save(){setSaving(true);try{const res=await fetch("/api/admin/settings",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});if(!res.ok)throw new Error();toast.success("Configuración guardada")}catch{toast.error("No se pudo guardar la configuración")}finally{setSaving(false)}}return <div className="space-y-6"><section className="rounded-2xl border bg-card p-6"><div className="mb-6 flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-violet-500/10 text-violet-500"><Globe2 className="size-5"/></span><div><h2 className="font-bold">Identidad de la academia</h2><p className="text-sm text-muted-foreground">Nombre y propuesta pública de la plataforma.</p></div></div><div className="grid gap-5 sm:grid-cols-2"><label><span className="mb-2 block text-sm font-semibold">Nombre</span><Input value={form.academyName} onChange={e=>set("academyName",e.target.value)}/></label><label><span className="mb-2 block text-sm font-semibold">URL pública</span><Input value={form.baseUrl} onChange={e=>set("baseUrl",e.target.value)} placeholder="https://academy.example.com"/></label></div><label className="mt-5 block"><span className="mb-2 block text-sm font-semibold">Tagline</span><Input value={form.tagline} onChange={e=>set("tagline",e.target.value)}/></label><label className="mt-5 block"><span className="mb-2 block text-sm font-semibold">Descripción</span><Textarea value={form.description??""} onChange={e=>set("description",e.target.value)} rows={4}/></label></section><section className="rounded-2xl border bg-card p-6"><div className="mb-6 flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-sky-500/10 text-sky-500"><Mail className="size-5"/></span><div><h2 className="font-bold">Correo y newsletter</h2><p className="text-sm text-muted-foreground">Configura el remitente de las nuevas publicaciones.</p></div></div><div className="grid gap-5 sm:grid-cols-3"><label><span className="mb-2 block text-sm font-semibold">Nombre del remitente</span><Input value={form.senderName} onChange={e=>set("senderName",e.target.value)}/></label><label><span className="mb-2 block text-sm font-semibold">Correo verificado</span><Input type="email" value={form.senderEmail} onChange={e=>set("senderEmail",e.target.value)}/></label><label><span className="mb-2 block text-sm font-semibold">Responder a</span><Input type="email" value={form.replyToEmail??""} onChange={e=>set("replyToEmail",e.target.value)}/></label></div><div className="mt-5 rounded-xl border border-amber-500/20 bg-amber-500/5 p-4 text-sm"><p className="flex items-center gap-2 font-semibold text-amber-500"><ShieldCheck className="size-4"/> Dominio verificado</p><p className="mt-1 text-xs leading-relaxed text-muted-foreground">Para enviar a cualquier suscriptor, el dominio de “Correo verificado” debe estar validado en Resend. `onboarding@resend.dev` funciona principalmente para pruebas.</p></div><label className="mt-5 flex items-center justify-between rounded-xl bg-muted p-4"><span className="flex items-center gap-3"><BellRing className="size-5 text-violet-500"/><span><b className="block text-sm">Notificar al publicar</b><small className="text-muted-foreground">Envía un correo una sola vez al pasar de borrador a publicado.</small></span></span><input type="checkbox" checked={form.notifyOnPublish} onChange={e=>set("notifyOnPublish",e.target.checked)} className="size-5 accent-violet-600"/></label></section><div className="flex justify-end"><Button onClick={save} disabled={saving} className="bg-violet-600 text-white"><Save className="mr-2 size-4"/> Guardar configuración</Button></div></div>}
